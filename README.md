# grain-cli-hello-world

## Description
This is a POC project to demonstrate grain, uses javascript as WASM technology.

## Tech stack
- grain

## Docker stack
- grainlang/grain

## To run
`sudo ./install.sh -u`

## To stop (optional)
`sudo ./install.sh -d`

## To see help
`sudo ./install.sh -h`
